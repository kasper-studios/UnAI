[package]
name = "unai-cli"
version = "0.1.0"
edition = "2021"
description = "UnAI — a workspace-oriented runtime for autonomous AI agents (infrastructure CLI)"
license = "MIT"
default-run = "unai"

[[bin]]
name = "unai"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dialoguer = "0.11"